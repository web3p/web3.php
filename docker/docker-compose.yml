version: '3'

services:

    php:
        build:
            context: ./php
            dockerfile: Dockerfile-72
        volumes:
            - ../:/app
        tty: true

    ganache:
        build:
            context: ./ganache
            dockerfile: Dockerfile
        ports:
          - "8545:8545"
